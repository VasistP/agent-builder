# agent-builder capabilities

Generated by `python3 tools/capabilities.py` from live repo state —
never hand-edit. Adding a phase, reference or tool makes it appear here.

Not using Claude Code? Nothing here needs a skill mechanism. Every skill
is plain markdown: read `skills/<name>/SKILL.md` directly. See `AGENTS.md`.

## Ways to invoke

| Mode | Say something like | What happens |
|------|--------------------|--------------|
| full | "build me an agent for X" | phases 0-8 in order, human checkpoint between each |
| targeted | "just set up evals" / "just observability" / "just security" | that phase only, against your existing repo — no re-scaffolding |
| audit | "review our eval setup" / "red-team our agent" | scores what you have against the matching standards doc, returns prioritized gaps |
| override | "we need to skip X" | the only way to change a locked default |

Every phase below with an `audit` entry path can be pointed at an existing
codebase — you do not have to have used this framework to build it.

## Phases

| # | Skill | Entry paths | What it does |
|---|-------|-------------|--------------|
| 0 | `0-discovery` | build | Rigorously interview the user about the agent they want to build — questions in small batches, consensus before anything is writt… |
| 1 | `1-scaffold` | build | Create the project skeleton, dev tooling, docstring enforcement, the FUNCTIONS.md index and fn_search CLI, the agent context file… |
| 2 | `2-observability` | build, add, audit | Set up open-source agent observability chosen by agent type, always with a portable OpenTelemetry-GenAI JSON trace export, plus a… |
| 3 | `3-evalset` | build, add, audit | Co-author evaluation test sets with the user — separate single-response and conversation sets stored as JSONL — with mixed determ… |
| 4 | `4-testing` | build | Build deterministic, token-free unit / regression / integration test suites covering only non-LLM functions. |
| 5 | `5-agent-skeleton` | build | Build the minimal agent loop (LangGraph or chosen framework) with typed state, a tool registry, full docstrings, observability wi… |
| 6 | `6-feature` | build | Implement exactly ONE feature from the backlog, eval-gated, with a mandatory human checkpoint before merge. |
| 7 | `7-security` | build, add, audit | Harden an AI agent against prompt injection, excessive agency, memory poisoning and unbounded blast radius, per the OWASP Top 10 … |
| 8 | `8-adversarial` | build, add, audit | Adversarially test an agent — run a red-team attack corpus, hold a live probing session with the user, and turn every finding int… |

## Standalone skills — call these any time

| Skill | What it does |
|-------|--------------|
| `capabilities` | Show everything agent-builder can do — every phase, which ones can be called standalone or pointed at an existing codebase, the C… |
| `override` | The ONLY way to change any default, structure, or configuration defined by agent-builder — including the evals and observability … |

## Tools in every scaffolded project

| Tool | Purpose |
|------|---------|
| `tools/arch_snapshot.py` | Guard the snapshot discipline for docs/ARCHITECTURE.md. |
| `tools/check_integrations.py` | Report which optional MCP servers and skills are available, and enable them on demand. |
| `tools/context_budget.py` | Keep the agent context files small enough to stay cheap to read. |
| `tools/fn_search.py` | Ranked search over function names + docstrings in src/. |
| `tools/functions_index.py` | Generate FUNCTIONS.md: an index of every function in src/ with its summary. |
| `tools/hook_advice.py` | Turn a Claude Code hook payload into a model-routing advisory. |
| `tools/route_task.py` | Decide which model tier a task should run on, per .agent/model-policy.yml. |

## Make targets

| Target | Purpose |
|--------|---------|
| `make help` | List targets |
| `make setup` | Install deps (uv) and pre-commit hook |
| `make lint` | Ruff (incl. mandatory docstrings) + mypy + context budgets |
| `make test` | All deterministic suites (no tokens) |
| `make test-unit` | Pure-function unit tests |
| `make test-regression` | Golden-file transform tests |
| `make test-integration` | Deterministic wiring vs real data-store containers (no model calls) |
| `make context-budget` | Check agent context files are within their line budgets |
| `make context-rotate` | Archive overflow CHANGELOG/TODO entries into docs/archive/ |
| `make arch-snapshot` | Verify ARCHITECTURE.md is committed before you rewrite it |
| `make arch-snapshot-commit` | Commit pending ARCHITECTURE.md changes (never pushes) |
| `make arch-history` | List past versions of ARCHITECTURE.md |
| `make integrations` | What optional MCPs/skills are available, and what would they unlock? |
| `make integrations-enable` | Add one catalogued MCP server:  make integrations-enable NAME=context7 |
| `make route` | Which model tier should a task use?  make route T="find the parser" |
| `make functions-index` | Regenerate FUNCTIONS.md from src/ docstrings |
| `make tools-posture` | Show every tool's permission, side-effect flag and where it's declared |
| `make eval-coverage` | Check the eval suite against the golden standard (no tokens) |
| `make eval-coverage-golden` | The phase 8 gate: full golden standard, blocking |
| `make eval` | Run the eval suite (local Ollama judge by default) |
| `make adversarial` | Full red-team suite — any breach fails the build |
| `make adversarial-fast` | High-signal adversarial subset (for pre-commit / PR) |
| `make obs-up` | Start observability stack (docker compose) |
| `make obs-down` | Stop observability stack |
| `make smoke` | Emit a fake traced agent run to verify observability |

## MCP servers — catalogued, installed on demand

Nothing is installed by default. `make integrations` shows status;
`make integrations-enable NAME=x` adds one, version-pinned.

| Server | Enables |
|--------|---------|
| `context7` | Up-to-date library and framework docs, so the agent stops guessing stale APIs. |
| `playwright` | Browser automation — dashboard E2E tests and web-interacting agents. |
| `memory` | Cross-session knowledge graph so a new session keeps prior decisions. |
| `sequential-thinking` | Structured multi-step reasoning for complex planning. |

## Companion skills — used if available, never required

Each has a fallback in `.agent/integrations.yml`, so a missing skill
degrades the step rather than skipping it.

| Skill | Enables |
|-------|---------|
| `dataviz` | Dashboard chart design — palette, accessibility, light/dark, layout. |
| `code-review` | Review the diff at the pre-merge checkpoint. |
| `security-review` | Security review over the changed code in phase 7. |
| `claude-api` | Authoritative model ids, pricing and limits for evals/pricing.json. |
| `mcp-builder` | Build a narrow, purpose-built MCP server for an internal system. |
| `skill-creator` | Turn team conventions into reusable skills once the POC stabilizes. |

## Reference docs — read on demand

| File | Covers |
|------|--------|
| `references/adversarial-standards.md` | Adversarial testing standards (build + audit rubric) |
| `references/context-and-cost.md` | Context files and token cost control |
| `references/deterministic-testing.md` | Deterministic testing philosophy |
| `references/eval-authoring-guide.md` | Eval authoring guide |
| `references/eval-standards.md` | Evaluation standards (build + audit rubric) |
| `references/interview-protocol.md` | Interview protocol |
| `references/mcp-catalogue.md` | MCP servers and companion skills |
| `references/methodology.md` | Development methodology: TDD for deterministic code, tiered EDD for model behavior |
| `references/observability-options.md` | Observability tool options (open-source only) |
| `references/observability-standards.md` | Observability standards (build + audit rubric) |
| `references/override-registry.md` | Override registry |
| `references/security-standards.md` | Agent security standards (build + audit rubric) |
| `references/stack-options.md` | Stack options |
| `references/tool-design.md` | Tool design and runtime context management |

## Out of scope

Regulatory compliance and data governance, per-user data authorization
(ACL-aware retrieval), and production deployment or secrets management.
Real concerns for enterprise agents — they belong to compliance and
platform owners, and this framework names them rather than
half-implementing them.
