"""Agent package: state, model shim, graph, and the run entrypoint.

`skills/5-agent-skeleton` fills in the real graph; this ships a minimal loop so
the environment (observability + evals + tests) is verifiable end to end.
"""
