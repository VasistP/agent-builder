# Override ledger

Every change to an agent-builder default, made via `skills/override`. Nothing
here should ever be edited by hand — if a row is wrong, run the override skill.

Declined requests are recorded too, so the same decision doesn't get
re-litigated later.

| Date | Item | Tier | From → To | Scope / Expiry | Verdict | Accepted consequence | Status | Revert |
|------|------|------|-----------|----------------|---------|----------------------|--------|--------|
| _none yet_ | | | | | | | | |

**Status** is one of: `active`, `expired`, `reverted`, `declined`.

## Active gaps

Summarize here what is currently NOT being checked as a result of the active
rows above. Keep this honest — it's the thing a new team member should read
first.

_No active overrides. All framework defaults are in force._
